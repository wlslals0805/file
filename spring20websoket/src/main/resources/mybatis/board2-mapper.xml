<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
    
    <mapper namespace="board">
  <insert id="boardAdd">
    insert into board2(board_no,member_id,board_content) 
    values(board2_seq.nextval,#{memberId},#{boardContent})
    
    </insert>
    
    </mapper>